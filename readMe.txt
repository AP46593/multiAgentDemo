# ─── system dependencies ─────────────────────────────────────────────
# install Graphviz system package from website or package manager    
# install Ollama server, see https://ollama.com/docs/installation

# python packages
# pip install --upgrade pip
# pip install -r requirements.txt
# pip install ollama==0.0.5


# system packages
#   • Graphviz  (dot -V should now work)
#   • Ollama    (ollama serve should start)

# 4. model pulls
# ollama pull llama3:8b
# ollama pull llava:13b      # or llava:7b-v1.6-mistral